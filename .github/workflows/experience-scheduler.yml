name: Experience Scheduler

on:
  schedule:
    - cron: "0 * * * *"   # 每小時執行一次
  workflow_dispatch:

jobs:
  run-scheduler:
    runs-on: ubuntu-latest

    steps:
      - name: Call Experience Scheduler API
        run: |
          curl -X POST https://YOUR_DOMAIN/api/scheduler/experience
