- name: Call Snapshot Scheduler API
        run: |
          curl -f -X POST "https://www.mosport.app/api/scheduler/run" \
            -H "Content-Type: application/json" \
            -d "{\"secret\": \"${{ secrets.CRON_SECRET }}\"}"
